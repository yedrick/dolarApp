<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         stopOnFailure="false">

    <testsuites>
        <!-- Suite completa -->
        <testsuite name="All">
            <directory>tests</directory>
        </testsuite>

        <!-- Pruebas unitarias (rápidas, sin BD) -->
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>

        <!-- Pruebas de integración HTTP (API + Web Blade) -->
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>

        <!-- Pruebas de vistas web (Blade + sesión) -->
        <testsuite name="Web">
            <directory>tests/Feature/Web</directory>
        </testsuite>

        <!-- Pruebas de aceptación (flujos de negocio completos) -->
        <testsuite name="Acceptance">
            <directory>tests/Acceptance</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <!-- Excluir capas que no necesitan cobertura directa -->
            <directory>app/Infrastructure/Models</directory>
        </exclude>
    </source>

    <php>
        <env name="APP_ENV"       value="testing"/>
        <env name="APP_KEY"       value="base64:TestKey32BytesLongForDolarApp==" />
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE"   value=":memory:"/>
        <env name="CACHE_DRIVER"  value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER"   value="array"/>
        <env name="MAIL_MAILER"      value="array"/>
        <env name="BCRYPT_ROUNDS"    value="4"/>
    </php>

    <coverage>
        <report>
            <html outputDirectory="coverage-report" lowUpperBound="50" highLowerBound="80"/>
            <text outputFile="php://stdout" showUncoveredFiles="false"/>
        </report>
    </coverage>

</phpunit>
